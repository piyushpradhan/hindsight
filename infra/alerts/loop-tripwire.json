{
  "//": "UNINSTALLED TEMPLATE. Trace-correlated loop event rule, target SigNoz v0.133.x.",
  "hindsightInstallStatus": "template_uninstalled",
  "alert": "Hindsight: recorded agent loop",
  "alertType": "LOGS_BASED_ALERT",
  "ruleType": "threshold_rule",
  "version": "v5",
  "schemaVersion": "v2alpha1",
  "evalWindow": "1m0s",
  "frequency": "30s",
  "severity": "critical",
  "condition": {
    "compositeQuery": {
      "queryType": "builder",
      "panelType": "graph",
      "queries": [
        {
          "type": "builder_query",
          "spec": {
            "name": "A",
            "signal": "logs",
            "source": "",
            "stepInterval": 0,
            "aggregations": [
              { "expression": "count()" }
            ],
            "filter": {
              "expression": "hindsight.event.name = 'loop_detected'"
            },
            "groupBy": [
              { "name": "trace_id" },
              { "name": "hindsight.agent.id" },
              { "name": "hindsight.run.id" },
              { "name": "hindsight.tool.name" }
            ],
            "having": { "expression": "" },
            "disabled": false
          }
        }
      ]
    },
    "selectedQueryName": "A",
    "thresholds": {
      "kind": "basic",
      "spec": [
        {
          "name": "critical",
          "target": 0,
          "targetUnit": "",
          "recoveryTarget": null,
          "matchType": "1",
          "op": "1",
          "channels": ["hindsight-replay-engine"]
        }
      ]
    }
  },
  "evaluation": {
    "kind": "rolling",
    "spec": {
      "evalWindow": "1m0s",
      "frequency": "30s"
    }
  },
  "summary": "Agent $hindsight.agent.id looped in $hindsight.tool.name, run $hindsight.run.id, trace $trace_id",
  "description": "A trace-correlated loop event opened a Hindsight incident at the affected run.",
  "labels": {
    "team": "hindsight",
    "trigger": "loop_detected"
  },
  "notificationSettings": {
    "groupBy": ["trace_id", "hindsight.agent.id", "hindsight.run.id", "hindsight.tool.name"],
    "usePolicy": false
  },
  "disabled": false
}
